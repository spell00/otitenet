{
  "name": "otitenet-desktop",
  "private": true,
  "version": "0.9.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "prepare:compact": "../scripts/prepare_desktop_variant.sh compact",
    "prepare:exact": "../scripts/prepare_desktop_variant.sh exact",
    "sidecar:build": "../scripts/build_streamlit_sidecar.sh",
    "sidecar:build:compact": "../scripts/build_streamlit_sidecar.sh compact",
    "sidecar:build:exact": "../scripts/build_streamlit_sidecar.sh exact",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:compact": "../scripts/build_tauri_variant.sh compact",
    "tauri:build:exact": "../scripts/build_tauri_variant.sh exact",
    "tauri:build:windows:compact": "../scripts/build_tauri_windows.sh compact",
    "tauri:build:windows:exact": "../scripts/build_tauri_windows.sh exact"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.4.0",
    "typescript": "^5.5.0"
  }
}
